# TeamHub 已实现功能清单

> 最后更新：2026-05-24
> 本文档基于当前代码库实际实现编写，与 `软件工程方案设计.md` 互为补充。

---

## 一、系统架构

| 层级 | 技术栈 |
|------|--------|
| 后端 | Spring Boot 3.5.14 + Java 17 + MyBatis Plus 3.5.12 + Spring Security JWT |
| 前端 | Vue 3.5.32 + Vite 8.0.8 + TypeScript ~6.0.0 + Element Plus 2.13.7 |
| 数据库 | MySQL 8 |
| API 文档 | Knife4j 4.4.0（`/doc.html`）|

**核心设计原则**：零文件存储。视频、图片全部使用外部网盘/图床链接，网站只存 URL 字符串。

**开发环境局域网访问**：Vite `host: '0.0.0.0'` + Proxy 代理后端，CORS 白名单覆盖 localhost / 127.0.0.1 / 10.x.x.x / 192.168.x.x / 云服务器公网地址。

---

## 二、功能模块详情

### 1. 认证与账户

| 功能 | 说明 |
|------|------|
| JWT 登录 | 用户名+密码，token 有效期 7 天，存储在 localStorage |
| 队长注册 | 填写球队名称 → 自动创建球队 + 生成 6 位大写邀请码（7 天有效）|
| 队员注册 | 填写邀请码 → 加入球队 |
| 修改密码 | 登录后首页点击「🔒 修改密码」按钮 → 弹窗输入原密码 + 新密码 + 确认密码 |
| 找回密码 | 登录页「忘记密码」→ 联系队长获取邀请码 → 输入用户名+邀请码+新密码重置 |
| 登录错误提示 | 密码错误或用户名不存在时，前端准确显示后端返回的具体错误信息 |

### 2. 首页 / 个人仪表盘

| 功能 | 说明 |
|------|------|
| 球员信息卡片 | 头像、姓名、号码、位置、自动计算的年级（本科/硕士/博士）|
| 我的进步方向 | 显示最新自评的分类 + 当前问题 + 改进目标 + 期望训练 |
| 本周训练 | 未来 7 天内的训练列表，显示时间、地点、当前报名状态、已结束标记 |
| 最近比赛 | 最近 3 场比赛，显示赛事类型、对手、时间、状态，可点击进入详情 |
| 出勤率 | 大字展示百分比，已参加 / 总训练次数 |
| 队友建议 | 待处理（PENDING）的队友建议，底部可跳转发展中心 |
| 球队建议（队长）| 待处理（PENDING）的球队建议，仅队长可见 |
| 修改密码 | 蓝色按钮 → 弹窗修改 |

### 3. 训练中心

| 功能 | 说明 |
|------|------|
| 训练列表 | 所有历史训练，显示标题、时间、地点、个人报名状态（已报名/请假/待定/未报名）、已结束标记 |
| 训练详情 | 信息展示 + 报名统计（参加/请假/待定人数）+ 我的报名操作 |
| 发布训练 | 仅队长，填写标题、地点、时间、内容、视频链接 |
| 报名截止机制 | 训练时间 3 小时后自动标记「已结束」，关闭报名修改通道 |

### 4. 比赛中心

| 功能 | 说明 |
|------|------|
| 比赛列表 | 所有比赛，支持按赛事类型筛选（全部/训练赛/新生杯/华工杯/其他），筛选栏横向滚动不换行 |
| 比赛详情 | 对手、时间、地点、赛事类型、多小局比分、首发名单、备注、视频链接 |
| 创建比赛 | 仅队长，填写基本信息 + 动态增加局数比分 |
| 状态流转 | 未开始 → 进行中 → 已结束（仅队长可改状态）|
| 开放编辑 | 比分、首发、备注、视频：所有登录用户均可编辑；状态流转按钮仅队长可见 |

### 5. 球员档案

| 功能 | 说明 |
|------|------|
| 双区展示 | 「当打之年」（ACTIVE）/「功勋人物」（ALUMNI）标签切换 |
| 年级自动计算 | 根据入学年份 + 学位（本科4年/硕士3年/博士3年）+ 当前日期自动推算 |
| 编辑资料 | 本人可编辑自己，队长可编辑所有人（号码、位置、照片链接、学位、入学年份）|
| 毕业迁移 | 队长一键将队员状态改为 ALUMNI，数据全部保留 |
| 队长标签 | 球员卡片和详情页上，队长显示红色「队长」标识 |
| 球员详情 | 展示完整档案，队长可见毕业迁移按钮 |

### 6. 发展中心

#### 6A. 个人发展

| 功能 | 说明 |
|------|------|
| 球员自评 | 分类（技术/体能/意识/态度/配合）+ 当前问题（必填）+ 改进目标（必填）+ 期望训练（选填）|
| 历史自评 | 时间轴形式展示历次自评 |
| 队友建议墙 | 收到的建议，可标记状态（已采纳/尝试中/暂不适用），可举报 |

#### 6B. 全队自评

| 功能 | 说明 |
|------|------|
| 球员卡片网格 | 所有 ACTIVE 球员的头像、姓名、号码、位置、最新自评摘要 |
| 队员详情弹窗 | 点击卡片 → 自评历史 + 队友建议 + 给TA提建议（匿名可选）|

#### 6C. 球队建议

| 功能 | 说明 |
|------|------|
| 建议墙 | 所有人可提交，分类（训练内容/时间安排/战术方向/装备后勤/其他）+ 紧急程度 |
| 筛选 | 按分类、状态（已阅待议/采纳/暂不采纳/已落实）筛选 |
| 队长回应 | 选择状态 + 填写回应理由 |
| 点赞 | 每个人只能点一次，再点取消 |

#### 6D. 管理后台（仅队长）

| 功能 | 说明 |
|------|------|
| 被举报建议审核 | 表格展示被举报的个人建议，支持「忽略举报」和「删除」|

> 注意：管理后台当前**仅实现被举报建议审核**。重置队员密码功能通过「找回密码」流程（邀请码重置）完成，队长在管理后台无直接重置密码的界面。

### 7. 时光轴

| 功能 | 说明 |
|------|------|
| 时间河流 | 按时间倒序，自动事件灰色简洁，人工事件蓝色突出 |
| 年份/月份筛选 | 下拉框快速定位 |
| 发布人工事件 | 标题 + 描述 + 事件时间 + 关联训练/比赛/球员（选填）+ 图片链接（最多3张）+ 相册链接 |
| 详情/编辑 | 点击事件标题查看详情；人工事件可编辑，自动事件只读 |
| 自动事件 | 训练创建、比赛结束、新成员入队、功勋退役时系统自动插入 |
| 球队隔离 | 每个球队只能看到自己球队的事件 |

> 注意：时光轴中点击关联的训练/比赛/球员链接时，目前仅做列表页跳转（如 `/training`），未实现带 ID 的精准跳转。

### 8. 球队设置（仅队长）

| 功能 | 说明 |
|------|------|
| 邀请码管理 | 查看当前邀请码、有效期、剩余时间，支持重新生成 |
| 修改队名 | 输入新队名，点击保存 |
| 队长传承 | 点击按钮 → 弹窗选择队员 → 确认传承。老队长降级为队员，新队长升级，需重新登录 |
| 成员管理 | 弹窗表格展示所有成员，队长可编辑任意成员的全部信息（姓名/学历/入学年份/号码/位置），也可直接删除成员 |
| 解散球队 | 仅当球队只剩队长一人时可解散，删除球队及所有关联数据，清登录态 |

---

## 三、响应式与移动端适配

| 设备 | 表现 |
|------|------|
| 桌面端 | 左侧固定导航栏（可折叠为图标模式），顶部标题栏显示折叠按钮 |
| 移动端（< 768px）| 左侧导航栏隐藏，顶部显示汉堡按钮（≡），点击弹出抽屉式菜单；所有内容区单列排列 |

---

## 四、数据库表结构

共 **13 张表**，均含 `team_id` 字段（多球队隔离）：

| 表名 | 说明 |
|------|------|
| `user` | 用户（用户名、密码、角色、球队ID）|
| `player` | 球员档案（姓名、号码、位置、入学年份、学位、照片、状态）|
| `team` | 球队（名称、队长ID、邀请码、有效期）|
| `training` | 训练（标题、地点、时间、内容、视频链接）|
| `training_signup` | 训练报名（训练ID、用户ID、状态、请假理由）|
| `match_game` | 比赛（对手、时间、地点、赛事类型、状态、首发、备注、视频）|
| `match_set` | 比赛小局比分（比赛ID、局数、我方比分、对方比分）|
| `player_self_review` | 球员自评（用户ID、分类、当前问题、改进目标、期望训练）|
| `player_suggestion` | 个人建议（目标用户、建议人、匿名、分类、内容、状态、点赞数、举报标记）|
| `team_suggestion` | 球队建议（建议人、分类、紧急程度、内容、状态、队长回应、点赞数）|
| `player_suggestion_like` | 个人建议点赞记录（用户ID、建议ID）|
| `team_suggestion_like` | 球队建议点赞记录（用户ID、建议ID）|
| `timeline_event` | 时光轴事件（类型、标题、描述、图片、相册、关联ID、球队ID、事件时间）|

---

## 五、API 概览

### 主要 Controller 与端点

| Controller | 核心端点 | 说明 |
|------------|----------|------|
| `UserController` | `/api/user/login`, `/api/user/register`, `/api/user/password`, `/api/user/reset-password` | 认证与密码管理 |
| `PlayerController` | `/api/player/list`, `/api/player/{id}`, `/api/player/{id}/graduate`, `/api/player/{id}`(DELETE) | 球员档案与成员管理 |
| `TrainingController` | `/api/training/list`, `/api/training/{id}`, `/api/training/create`, `/api/training/{id}/signup` | 训练中心 |
| `MatchController` | `/api/match/list`, `/api/match/{id}`, `/api/match/create`, `/api/match/{id}/status` 等 | 比赛中心 |
| `DevelopmentController` | `/api/development/self-review`, `/api/development/player-suggestion`, `/api/development/team-suggestion` 等 | 发展中心 |
| `TimelineController` | `/api/timeline/list`, `/api/timeline`, `/api/timeline/{id}` | 时光轴 |
| `TeamController` | `/api/team/{id}`, `/api/team/refresh-invite`, `/api/team/name`, `/api/team/transfer-captain`, `/api/team/disband` | 球队管理 |
| `DashboardController` | `/api/dashboard` | 首页仪表盘聚合数据 |
| `TestController` | `/test` | 数据库连接测试（内部使用）|

### 权限控制

- **公开接口**：登录、注册、找回密码、Knife4j 文档
- **登录用户接口**：查看各类列表/详情、报名训练、编辑比赛信息（比分/首发/备注/视频）、提交建议、点赞
- **队长专属接口**：发布训练、创建比赛、修改比赛状态、毕业迁移、删除成员、修改队名、传承队长、解散球队、审核举报

---

## 六、已知问题与改进方向

### 高优先级

| 方向 | 现状 | 改进方案 |
|------|------|----------|
| **分页加载** | 训练列表、比赛列表、时光轴均为一次性全量加载 | 后端添加 `Page` 分页查询，前端接入 `el-pagination` |
| **图床集成** | 所有图片字段均为手动粘贴外部 URL，门槛高 | 前端集成免费图床 API（如 SM.MS），上传后自动回填 URL |
| **训练/比赛前提醒** | 无提醒机制 | 后端添加 `@Scheduled` 定时任务，训练前 N 小时推送提醒（需接入邮件/短信/微信）|

### 中优先级

| 方向 | 现状 | 改进方案 |
|------|------|----------|
| **富文本编辑器** | 比赛备注、训练内容、时光轴描述均为纯文本 textarea | 集成 Markdown 编辑器（如 mavon-editor）或轻量富文本编辑器 |
| **数据导出** | 无法导出任何数据 | 训练出勤统计导出 Excel、比赛记录导出 PDF、队员名单导出 CSV |
| **操作日志** | 无操作记录 | 新增 `operation_log` 表，记录关键操作（修改比分、删除建议、毕业迁移等）|
| **角色扩展** | 仅队长/队员两种角色 | 增加副队长、经理等角色，配置更细粒度的权限 |
| **管理后台补全** | 管理后台仅有举报审核 | 补充队长直接重置队员密码的界面 |

### 低优先级

| 方向 | 现状 | 改进方案 |
|------|------|----------|
| **数据统计图表** | 仅简单出勤率数字（ECharts 已安装但零引用）| 接入 ECharts 展示球队出勤率趋势、比赛胜率、个人成长曲线 |
| **前端状态管理** | Pinia 仅存在示例模板 `counter.ts`，项目中零引用 | 建立 `userStore`、`teamStore` 等，缓存球员列表等不常变数据 |
| **数据备份** | 无自动备份 | 定时任务导出 SQL 备份文件到指定目录 |
| **深色模式** | 仅浅色主题 | Element Plus 内置 dark 模式切换 |
| **时光轴精准跳转** | 点击关联链接仅跳到列表页 | 改为带 ID 的路由跳转（如 `/training/${id}`）|

---

## 七、前端工程化

| 项 | 配置 |
|----|------|
| 构建工具 | Vite 8.0.8 |
| 类型检查 | vue-tsc + TypeScript ~6.0.0 |
| 代码检查 | ESLint 10 + Oxlint 1.60（`npm run lint` 串联执行）|
| 图标库 | `@element-plus/icons-vue` |
| Node 版本 | 20.19.0 或 >= 22.12.0 |
