# TeamHub — 院队数字档案系统

## 《软件工程》项目报告（详细框架版）

> 本框架在上一版目录结构基础上，为每一章提供了可直接参考的写法模板、表格和示例段落。你只需把括号里的提示替换成你们项目的实际内容即可。

---

# 封面

```
《软件工程》项目报告

题目：        TeamHub — 院队数字档案系统

课程名称：    软件工程
专业班级：    （填写，如计算机科学与技术 2101 班）
组    名：    （填写）
同组成员：    学号：U20xxxxxxxx    姓名：xxx
              学号：U20xxxxxxxx    姓名：xxx
              学号：U20xxxxxxxx    姓名：xxx
指导教师：    （填写）
报告日期：    2026 年 5 月 25 日

计算机科学与技术学院
```

---

# 任务书

## 一 总体要求

1. 综合运用软件工程的思想，协同完成一个软件项目的开发，掌握软件工程相关的技术和方法；
2. 组成小组进行选题，通过调研完成项目的需求分析，并详细说明小组成员的分工、项目的时间管理等方面；
3. 根据需求分析进行总体设计、详细设计、编码与测试等。

## 二 基本内容

根据课程要求，本项目的软件过程包括以下五个部分：

1. **问题概述、需求分析（20%）**：使用 NABCD 模型说明项目的价值与目标，使用 Microsoft Visio、StarUML 等工具绘制 UML 需求分析图；
2. **原型系统设计、概要设计、详细设计（35%）**：说明所开发软件的架构、数据结构及主要算法设计，使用墨刀或 Figma 进行原型设计；
3. **编码与测试（30%）**：遵循编码规范，使用 Gitee 进行版本管理，设计测试计划和测试用例；
4. **功能创新（10%）**：设计与实现与众不同、特别吸引用户的功能；
5. **用户反馈（5%）**：收集用户的使用记录、照片、视频等反馈材料。

---

# 目录（自动生成）

---

# 1 问题定义

## 1.1 项目背景与意义

### 1.1.1 项目背景

学生体育团队（如院篮球队、院排球队等）是高校校园文化的重要组成部分。在日常运营中，球队队长需要组织训练、安排比赛、管理队员信息，而队员则需要了解训练安排、报名参赛、追踪个人成长。然而，目前大多数学生球队仍然依赖微信群或 QQ 群进行日常沟通与管理，这种传统模式存在以下突出问题：

（1）**信息碎片化**：训练通知、比赛结果、队员请假等信息散落在群聊记录中，历史信息难以检索，新入队队员无法快速了解球队历史；

（2）**考勤无记录**：训练报名与出勤情况依靠队长人工统计，既容易遗漏，也无法长期存档，学期末无法准确计算个人出勤率；

（3）**比赛战绩无结构化归档**：比赛结果通常仅以文字形式在群中播报，多局比分、首发名单、战术总结等关键信息没有统一记录，导致球队历史战绩随毕业流失；

（4）**球员成长无追踪**：队员的技术进步、训练反馈、个人发展目标等缺乏系统化记录，无法形成可回顾的成长轨迹；

（5）**球队历史无传承**：已毕业队员的信息和贡献没有被妥善保存，球队文化难以代际传承。

目前市面上虽有一些通用团队协作工具（如腾讯文档、石墨文档），但它们并非针对体育团队场景设计，缺乏训练报名、比赛战绩结构化、球员档案分级管理、球队编年史等核心功能。因此，开发一款专门面向学生体育团队的数字档案管理系统具有明确的现实需求。

### 1.1.2 项目意义

TeamHub 院队数字档案系统的开发，旨在为学生体育团队提供一套轻量级、零成本的数字化管理方案，其意义体现在以下三个层面：

**对队员而言**，系统提供训练考勤永久存档、比赛履历结构化记录、个人自评与成长追踪等功能，帮助每位队员建立完整的"数字球员卡"，清晰看见自己的进步轨迹。

**对队长而言**，系统简化训练发布与报名统计流程，支持比赛多局比分动态录入、首发名单管理、球队建议民主收集，大幅降低管理成本，提升决策效率。

**对球队而言**，时光轴功能自动记录球队重大事件（训练、比赛、新成员入队、功勋退役），形成可追溯的球队编年史，确保球队文化不因人员更替而断层。

### 1.1.3 竞品分析

| 竞品 | 核心功能 | 主要不足 |
|------|----------|----------|
| 微信群/QQ群 | 即时通讯、通知发布 | 信息碎片化、无法结构化存档、无权限管理 |
| 腾讯文档 | 在线表格、协同编辑 | 缺乏训练报名、比赛战绩模板、无角色权限 |
| 钉钉/飞书 | 考勤打卡、审批流 | 功能过重、配置复杂、不适合学生团队 |
| TeamHub（本项目） | 训练/比赛/球员/时光轴一体化 | — |

现有竞品普遍存在功能泛化、操作复杂或缺乏体育场景针对性的问题，TeamHub 通过聚焦学生体育团队的核心痛点，提供轻量、专注的解决方案。

## 1.2 项目基本目标

本项目的基本目标是设计与开发一款面向学生体育团队的 Web 应用系统——TeamHub，具体目标分解如下：

（1）**训练中心**：支持队长发布训练、队员接龙报名（参加/请假/待定）、自动统计出勤率、训练结束后自动锁定报名；

（2）**比赛中心**：支持队长创建比赛、动态录入多小局比分、记录首发名单与比赛备注、管理比赛状态流转（未开始→进行中→已结束）；

（3）**球员档案**：支持双区展示（当打之年/功勋人物）、年级自动计算、自助编辑资料、队长一键毕业迁移；

（4）**发展中心**：支持球员自评（技术/体能/意识/态度/配合）、队友匿名建议、球队建议墙与队长回应；

（5）**时光轴**：自动记录系统事件（训练创建、比赛结束、成员变动），支持人工发布事件，形成球队编年史；

（6）**球队设置**：支持邀请码管理、队名修改、队长传承、成员管理、解散球队。

系统采用前后端分离架构，遵循"零文件存储"原则（图片/视频使用外部链接），降低运维成本，适合学生团队长期使用。

## 1.3 可行性分析

### 1.3.1 技术可行性

本项目采用成熟的主流技术栈，技术风险可控：

- **后端**：Spring Boot 3.5 + MyBatis Plus + Spring Security + JWT，生态完善，文档丰富，适合快速开发 RESTful API；
- **前端**：Vue 3 + Vite + TypeScript + Element Plus，组件库成熟，响应式适配效率高；
- **数据库**：MySQL 8，关系型数据模型与业务场景高度契合；
- **部署**：云服务器 + Nginx 反向代理，配置简单，成本低廉。

小组成员已具备 Java、Vue 及数据库开发基础，能够独立完成全栈开发工作。

### 1.3.2 经济可行性

- **开发成本**：所有技术框架均为开源免费，无商业授权费用；开发设备为个人电脑，无额外硬件投入；
- **部署成本**：单台云服务器即可承载全队使用；图片与视频采用外部网盘/图床链接，不占用服务器存储与带宽；
- **收益分析**：系统直接提升球队管理效率，减少人工统计时间，隐性收益远大于投入成本。

### 1.3.3 操作可行性

目标用户为高校学生，日常已熟练使用微信、QQ 等社交应用。TeamHub 的界面设计遵循 Element Plus 设计规范，操作逻辑直观，无需专门培训即可上手。同时，系统采用响应式布局，支持桌面端与移动端浏览器访问，适应多种使用场景。

## 1.4 人员管理和项目进度管理

### 1.4.1 人员分工

本项目小组共 X 人，采用分工协作模式，具体分工如下：

| 姓名 | 学号 | 主要职责 |
|------|------|----------|
| XXX | U20xxxxxxxx | 后端架构设计、数据库设计、RESTful API 开发、服务器部署与运维 |
| XXX | U20xxxxxxxx | 前端页面开发、用户交互设计、响应式布局适配、UI 原型设计 |
| XXX | U20xxxxxxxx | 需求分析、测试用例设计、文档撰写、用户调研与反馈收集 |

### 1.4.2 项目进度管理

本项目采用增量式开发模型，总体分为六个阶段，各阶段时间安排如下：

| 阶段 | 时间 | 主要任务 | 交付物 |
|------|------|----------|--------|
| 需求分析 | 第 1–2 周 | 调研球队管理痛点、撰写 NABCD 分析、绘制 UML 图、设计原型 | 需求规格说明书、原型设计稿 |
| 概要设计 | 第 3 周 | 确定系统架构、设计数据库表结构、规划接口规范 | 概要设计文档、E-R 图 |
| 详细设计 | 第 4 周 | 设计关键算法流程、细化模块接口、确定编码规范 | 详细设计文档、流程图 |
| 编码实现 | 第 5–7 周 | 后端 API 开发、前端页面开发、前后端联调 | 可运行的系统原型 |
| 测试与优化 | 第 8–9 周 | 单元测试、集成测试、Bug 修复、性能优化 | 测试报告、修复记录 |
| 部署与验收 | 第 10 周 | 服务器部署、域名配置、用户试用、文档定稿 | 上线系统、项目报告 |

（配图建议：甘特图）

---

# 2 需求分析

## 2.1 需求分析概述

### 2.1.1 NABCD 模型分析

**N（Need，需求）**

学生体育团队在日常管理中面临信息碎片化、考勤无记录、战绩无归档、成长无追踪、历史无传承等痛点。队长需要一款能够结构化管理训练、比赛、球员档案的工具；队员需要便捷地查看训练安排、报名参赛、追踪个人进步；球队整体需要一份可追溯的数字编年史。

**A（Approach，方法）**

基于 B/S 架构开发 TeamHub Web 应用，采用前后端分离技术方案。后端使用 Spring Boot 提供 RESTful API，前端使用 Vue 3 构建单页应用。系统支持 JWT 身份认证、双角色权限控制（队长/队员）、多球队数据隔离。核心功能包括训练中心、比赛中心、球员档案、发展中心、时光轴和球队设置六大模块。

**B（Benefit，好处）**

对队员：训练出勤自动统计、比赛履历永久保存、个人成长可量化追踪；对队长：一键发布训练/比赛、民主收集球队建议、高效管理成员信息；对球队：时光轴自动记录历史节点，功勋人物归档展示，文化传承不断层。零文件存储设计使系统无需额外存储成本，适合学生团队长期使用。

**C（Competitors，竞争）**

现有替代方案主要包括微信群/QQ群（信息碎片化）、腾讯文档/石墨文档（缺乏体育场景功能）、钉钉/飞书（功能过重）。TeamHub 的优势在于场景聚焦、功能闭环、轻量易用、零运维成本。

**D（Delivery，推广）**

首先在开发团队所在球队内部试用，通过实际使用验证功能价值；借助球队间的社交关系，通过邀请码机制自然扩散至其他院队；在高校体育类公众号或校园论坛分享使用体验，吸引同类团队关注。

### 2.1.2 产品功能描述

TeamHub 的功能架构如图 X-X 所示，分为六大核心模块：

（配图建议：功能模块思维导图或层次图）

## 2.2 UML 相关需求分析图

### 2.2.1 系统用例图

TeamHub 包含两类核心角色：普通队员（MEMBER）和队长（CAPTAIN）。

- **普通队员用例**：登录系统、查看训练列表、报名参加训练、查看比赛列表与详情、编辑个人球员资料、提交自评、提交队友建议、提交球队建议、查看时光轴、点赞建议。
- **队长用例**：包含普通队员全部用例，额外拥有：发布训练、创建/编辑比赛、修改比赛状态、编辑任意球员资料、毕业迁移队员、修改队名、传承队长、管理成员、解散球队、审核被举报建议。

（配图建议：用例图，包含两个角色方框和对应的用例椭圆）

### 2.2.2 时序图

以"队员报名参加训练"为例，其时序图说明如下：

1. 队员在前端训练详情页选择报名状态（参加/请假/待定）；
2. 前端发送 POST 请求至 `/api/training/{id}/signup`；
3. TrainingController 接收请求，调用 TrainingService 处理业务逻辑；
4. Service 层校验训练是否已结束（当前时间 > 训练时间 + 3小时则拒绝）；
5. 校验通过后，向 training_signup 表插入或更新记录；
6. 返回操作结果，前端更新报名统计展示。

（配图建议：时序图）

### 2.2.3 数据流图

**顶层图**：
- 外部实体：队员、队长；
- 加工：TeamHub 系统；
- 数据存储：MySQL 数据库；
- 数据流：登录信息、训练信息、比赛信息、球员信息、建议信息、事件信息。

**0 层图**：
将系统划分为六个加工：训练管理、比赛管理、球员档案管理、发展中心管理、时光轴管理、球队设置管理。

**1 层图（以训练管理为例）**：
- 加工 1.1：发布训练（仅队长）；
- 加工 1.2：接龙报名（队员）；
- 加工 1.3：报名统计与出勤率计算。

（配图建议：顶层图、0层图、1层图各一张）

### 2.2.4 系统 E-R 图

系统主要实体及其关系如下：

- **User**（用户）：id、username、password、role、team_id；
- **Player**（球员档案）：user_id、name、jersey_number、position、entry_year、degree、photo_url、status、team_id；
- **Team**（球队）：id、name、captain_id、invite_code、invite_code_expires_at；
- **Training**（训练）：id、title、location、train_time、content、video_url、team_id；
- **TrainingSignup**（训练报名）：id、training_id、user_id、status、reason；
- **MatchGame**（比赛）：id、opponent、game_time、location、game_type、status、lineup、notes、video_url、team_id；
- **MatchSet**（比赛小局）：id、match_id、set_number、our_score、opp_score；
- **PlayerSelfReview**（球员自评）：id、user_id、category、current_problem、improvement_goal、expected_training；
- **PlayerSuggestion**（队友建议）：id、target_user_id、from_user_id、is_anonymous、category、content、status、likes、is_reported；
- **TeamSuggestion**（球队建议）：id、from_user_id、category、urgency、content、status、captain_reply、likes；
- **TimelineEvent**（时光轴事件）：id、event_type、title、description、image_urls、album_url、ref_training_id、ref_match_id、ref_player_id、team_id、event_time。

实体关系：User 与 Player 一对一；Team 与 Player/Training/MatchGame/TimelineEvent 一对多；Training 与 TrainingSignup 一对多；MatchGame 与 MatchSet 一对多。

（配图建议：E-R 图）

## 2.3 原型系统设计

### 2.3.1 设计工具与思路

本项目使用 Figma / 墨刀进行原型设计。设计思路如下：

- **风格定位**：简洁、专业、运动感十足。主色调采用蓝色系（#409EFF），象征信任与稳定；辅色使用橙色点缀，增加活力感；
- **布局策略**：桌面端采用左侧固定导航栏 + 右侧内容区，支持折叠；移动端采用顶部汉堡按钮 + 抽屉式菜单，内容区单列排列；
- **交互策略**：弹窗优先于页面跳转，减少用户操作路径，保持上下文连贯。

### 2.3.2 核心界面展示

（以下每个功能配 1–2 张原型或实际系统截图，并附简要说明）

**（1）登录与注册界面**
- 支持队长创建球队（自动生成邀请码）和队员通过邀请码加入球队；
- 支持找回密码（通过邀请码验证重置）。

**（2）首页仪表盘**
- 展示球员信息卡片、本周训练提醒、最近比赛、出勤率、最新自评摘要、待处理建议；
- 队长额外展示待处理球队建议数量。

**（3）训练中心**
- 训练列表页：展示所有历史训练，标记个人报名状态和已结束状态；
- 训练详情页：展示训练信息、报名统计（参加/请假/待定人数）、接龙报名操作区。

**（4）比赛中心**
- 比赛列表页：支持按赛事类型筛选（全部/训练赛/新生杯/华工杯/其他）；
- 比赛详情页：展示多小局比分、首发名单、备注、视频链接、状态流转按钮（仅队长可见）。

**（5）球员档案**
- 双区标签切换：当打之年（ACTIVE）与功勋人物（ALUMNI）；
- 球员详情页：展示完整档案，队长可见毕业迁移按钮。

**（6）发展中心**
- 个人发展标签：自评表单、历史自评时间线、队友建议墙；
- 全队自评标签：球员卡片网格，点击弹出详情弹窗（自评历史 + 建议 + 提交建议）；
- 球队建议标签：建议墙、分类筛选、队长回应。

**（7）时光轴**
- 时间河流：按时间倒序排列，自动事件灰色简洁，人工事件蓝色突出；
- 支持按年份/月份筛选，点击事件标题查看详情。

**（8）球队设置（仅队长）**
- 邀请码管理、修改队名、队长传承、成员管理（编辑/删除）、解散球队。

---

# 3 概要设计和详细设计

## 3.1 系统结构

### 3.1.1 系统整体架构

TeamHub 采用 B/S 架构，前后端分离设计，整体架构如图 X-X 所示：

- **表现层**：Vue 3 单页应用，通过 Axios 与后端通信，使用 Element Plus 组件库构建界面；
- **网关层**：Nginx 反向代理，负责静态资源托管、请求转发、负载均衡；
- **业务层**：Spring Boot 应用，按模块划分为 Controller → Service → Mapper 三层；
- **数据层**：MySQL 8 关系型数据库，通过 MyBatis Plus 进行 ORM 映射。

（配图建议：系统架构层次图）

### 3.1.2 系统模块设计

系统按功能划分为八个模块：

| 模块 | 核心功能 |
|------|----------|
| 登录注册模块 | JWT 登录、队长注册（创建球队）、队员注册（邀请码加入）、找回密码 |
| 训练中心模块 | 发布训练、训练列表、训练详情、接龙报名、报名统计、出勤率计算 |
| 比赛中心模块 | 创建比赛、比赛列表、比赛详情、多小局比分、首发名单、状态流转 |
| 球员档案模块 | 球员列表（双区展示）、球员详情、编辑资料、毕业迁移 |
| 发展中心模块 | 球员自评、历史自评、队友建议、球队建议墙、点赞、举报、队长回应 |
| 时光轴模块 | 事件列表、发布人工事件、编辑事件、年份/月份筛选、自动事件插入 |
| 球队设置模块 | 邀请码管理、修改队名、队长传承、成员管理、解散球队 |
| 管理后台模块 | 被举报建议审核（忽略/删除） |

（配图建议：系统功能模块图）

### 3.1.3 后端接口设计

系统采用 RESTful API 设计风格，核心接口如下：

| 模块 | 接口示例 | 方法 | 功能 |
|------|----------|------|------|
| 认证 | /api/user/login | POST | 用户登录 |
| 认证 | /api/user/register | POST | 用户注册 |
| 训练 | /api/training/create | POST | 创建训练（队长）|
| 训练 | /api/training/{id}/signup | POST | 训练报名 |
| 比赛 | /api/match/create | POST | 创建比赛（队长）|
| 比赛 | /api/match/{id}/status | PUT | 修改比赛状态（队长）|
| 球员 | /api/player/{id}/graduate | PUT | 毕业迁移（队长）|
| 发展 | /api/development/self-review | POST | 提交自评 |
| 球队 | /api/team/transfer-captain | POST | 队长传承 |

## 3.2 UML 类图

### 3.2.1 核心实体类图

以训练中心模块为例，涉及的实体类及其关系如下：

- **Training** 实体：id、title、location、train_time、content、video_url、created_by、team_id；
- **TrainingSignup** 实体：id、training_id、user_id、status（ATTEND/ABSENT/PENDING）、reason；
- **Training** 与 **TrainingSignup** 为一对多关系；
- **TrainingController** 依赖 **TrainingService**；**TrainingService** 依赖 **TrainingMapper** 和 **TrainingSignupMapper**。

（配图建议：实体类图，包含属性、方法及关联关系）

### 3.2.2 核心模块类图

以比赛中心模块为例：

- **MatchGame** 与 **MatchSet** 一对多；
- **MatchController** 提供 createMatch、updateStatus、updateSets、updateLineup 等方法；
- **MatchService** 封装业务逻辑，处理比赛状态流转规则；
- **MatchGameMapper** / **MatchSetMapper** 负责数据库操作。

（配图建议：模块类图）

## 3.3 关键数据结构设计

### 3.3.1 核心数据库表结构

**（1）user 表（用户表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK，AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| password | VARCHAR(255) | NOT NULL | 加密后的密码 |
| role | VARCHAR(20) | NOT NULL | 角色：MEMBER / CAPTAIN |
| team_id | BIGINT | FK | 所属球队ID |

**（2）player 表（球员档案表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | BIGINT | PK, FK | 关联用户ID |
| name | VARCHAR(50) | NOT NULL | 球员姓名 |
| jersey_number | INT | — | 球衣号码 |
| position | VARCHAR(50) | — | 主打位置 |
| entry_year | INT | NOT NULL | 入学年份 |
| degree | VARCHAR(20) | NOT NULL | 学位：本科/硕士/博士 |
| photo_url | VARCHAR(255) | — | 头像图片链接 |
| status | VARCHAR(20) | NOT NULL | ACTIVE / ALUMNI |
| team_id | BIGINT | FK | 所属球队ID |

**（3）training 表（训练表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK，AUTO_INCREMENT | 训练ID |
| title | VARCHAR(100) | NOT NULL | 训练标题 |
| location | VARCHAR(100) | NOT NULL | 训练地点 |
| train_time | DATETIME | NOT NULL | 训练时间 |
| content | TEXT | — | 训练内容 |
| video_url | VARCHAR(255) | — | 动作视频链接 |
| team_id | BIGINT | FK | 所属球队ID |

**（4）training_signup 表（训练报名表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK，AUTO_INCREMENT | 报名ID |
| training_id | BIGINT | FK | 关联训练ID |
| user_id | BIGINT | FK | 关联用户ID |
| status | VARCHAR(20) | NOT NULL | ATTEND / ABSENT / PENDING |
| reason | VARCHAR(255) | — | 请假理由 |

（其余表略，报告中可选择 3–5 个核心表详细说明）

### 3.3.2 数据关联说明

**多球队隔离机制**：所有业务表均包含 `team_id` 字段，通过外键关联 team 表。每次查询时通过当前登录用户的 `team_id` 进行过滤，确保不同球队之间的数据完全隔离。

**时光轴关联机制**：timeline_event 表通过 `ref_training_id`、`ref_match_id`、`ref_player_id` 三个可选外键，分别关联训练、比赛、球员实体，实现人工事件与具体业务对象的挂靠。

## 3.4 关键算法设计

### 3.4.1 年级自动计算算法

**功能**：根据球员的入学年份和学位类型，自动计算并显示当前年级。

**输入**：entry_year（入学年份，如 2024）、degree（学位：本科/硕士/博士）、current_date（当前日期）。

**处理**：
1. 计算年份差：diff = current_year - entry_year；
2. 根据学位确定学制：本科 4 年、硕士 3 年、博士 3 年；
3. 若 current_month < 9（未过开学季），diff 减 1；
4. 年级文本映射：diff + 1 → "大一"/"研一"等；
5. 若 diff + 1 > 学制年限，显示"已毕业"。

**输出**：年级文本字符串（如"大二""研一""已毕业"）。

（配图建议：流程图）

### 3.4.2 JWT 认证与权限校验算法

**功能**：验证用户身份并控制接口访问权限。

**输入**：HTTP 请求头中的 Authorization 字段（Bearer Token）。

**处理**：
1. 从请求头中提取 Token；
2. 使用 JWT 密钥解析 Token，获取用户ID、用户名、角色；
3. 验证 Token 是否在有效期内；
4. 将解析后的用户信息存入 SecurityContext；
5. 对于标注 `@PreAuthorize("hasRole('CAPTAIN')")` 的接口，校验用户角色是否为 CAPTAIN。

**输出**：认证通过则放行请求；Token 无效返回 401；权限不足返回 403。

（配图建议：认证流程图）

### 3.4.3 训练报名截止判定算法

**功能**：判断某次训练是否已结束报名。

**输入**：training.train_time（训练时间）。

**处理**：
1. 计算截止时间：deadline = train_time + 3小时；
2. 比较当前时间与 deadline；
3. 若 current_time > deadline，则标记为已结束。

**输出**：布尔值（true：已结束，false：未结束）。

### 3.4.4 比赛状态流转算法

**功能**：控制比赛状态的合法变更。

**状态机定义**：
- 未开始 → 进行中（允许）
- 进行中 → 已结束（允许）
- 未开始 → 已结束（允许，跳过中间态）
- 已结束 → 任意状态（拒绝）

**处理**：队长调用状态更新接口时，校验目标状态是否为当前状态的合法后继，若非法则返回错误提示。

（配图建议：状态机图）

## 3.5 数据管理说明

### 3.5.1 数据存储方式

- **业务数据**：采用 MySQL 8 关系型数据库存储，利用事务保证数据一致性；
- **会话数据**：前端使用 localStorage 存储 JWT Token 和基本信息，后端为无状态设计；
- **多媒体数据**：遵循零文件存储原则，图片和视频均使用外部网盘/图床链接，系统中仅保存 URL 字符串。

### 3.5.2 数据安全策略

- **密码安全**：采用 BCrypt 算法对用户密码进行单向哈希存储，防止数据库泄露导致明文密码暴露；
- **接口安全**：敏感接口通过 Spring Security 方法级注解控制，仅允许特定角色访问；
- **SQL 注入防护**：使用 MyBatis Plus 参数化查询，避免字符串拼接 SQL；
- **跨域安全**：通过 CORS 配置限制允许访问的域名，防止恶意跨域请求。

### 3.5.3 数据备份

- 云服务器配置定时任务（crontab），每日凌晨自动执行 `mysqldump` 导出数据库备份；
- 备份文件保留最近 7 天，存储于 `/opt/teamhub/backup/` 目录。

---

# 4 实现与测试

## 4.1 实现环境与代码管理

### 4.1.1 应用实现环境

**前端开发环境**：
- 操作系统：Windows 11 / macOS；
- 开发工具：Visual Studio Code；
- 运行环境：Node.js 20.19.0+；
- 构建工具：Vite 8.0.8；
- 技术框架：Vue 3.5.32、Vue Router 5.0.4、Element Plus 2.13.7、Axios 1.15.2。

**后端开发环境**：
- 操作系统：Windows 11 / macOS；
- 开发工具：IntelliJ IDEA 2024.x；
- 运行环境：Java 17、Maven 3.9+；
- 技术框架：Spring Boot 3.5.14、MyBatis Plus 3.5.12、Spring Security、JWT（jjwt 0.12.6）。

**数据库与部署环境**：
- 数据库：MySQL 8.0；
- 服务器：腾讯云/阿里云 2核4G 云服务器；
- 操作系统：Ubuntu 22.04 LTS；
- Web 服务器：Nginx 1.24；
- 部署方式：前端静态文件托管 + Nginx 反向代理后端服务 + systemd 守护进程。

### 4.1.2 代码管理

本项目使用 Git 进行版本控制，代码仓库托管于 Gitee（或 GitHub）。

（配图建议：Gitee/GitHub 代码提交记录截图、贡献度统计图）

开发过程中采用功能分支工作流：
- `main` 分支：稳定版本；
- `feature/xxx` 分支：功能开发；
- `fix/xxx` 分支：Bug 修复。

## 4.2 关键函数说明

> 以下说明系统中核心模块的关键函数，完整代码详见附录。

### 4.2.1 训练报名统计函数

**函数名**：`getTrainingStats(Long trainingId)`

**所在类**：`TrainingService`

**功能说明**：查询指定训练的各项报名统计数据，包括参加人数、请假人数、待定人数。

**处理流程**：
1. 调用 `trainingSignupMapper.selectList` 查询该训练的所有报名记录；
2. 按 status 字段分组统计；
3. 封装为 `TrainingStatsVO` 对象返回。

**调用关系**：TrainingController → TrainingService → TrainingSignupMapper。

### 4.2.2 比赛状态更新函数

**函数名**：`updateMatchStatus(Long matchId, String status)`

**所在类**：`MatchService`

**功能说明**：队长修改比赛状态，若状态变为"已结束"，自动触发时光轴事件插入。

**处理流程**：
1. 校验当前用户是否为队长；
2. 校验目标状态是否为当前状态的合法后继；
3. 更新 match_game 表的状态字段；
4. 若新状态为 ENDED，调用 `timelineService.createAutoEvent` 插入"比赛结束"时光轴事件；
5. 返回更新结果。

**调用关系**：MatchController → MatchService → MatchGameMapper / TimelineService。

### 4.2.3 年级自动计算函数

**函数名**：`calculateGrade(Integer entryYear, String degree)`

**所在类**：`PlayerService` 或工具类

**功能说明**：根据入学年份和学位计算当前年级显示文本。

**处理流程**：
1. 获取当前日期；
2. 计算 rawDiff = currentYear - entryYear；
3. 若 currentMonth < 9，diff = rawDiff - 1，否则 diff = rawDiff；
4. 根据 degree 获取学制年限（本科4、硕士3、博士3）；
5. grade = diff + 1；
6. 若 grade > 学制，返回"已毕业"，否则返回对应年级文本。

**调用关系**：PlayerController / 前端展示时调用。

## 4.3 测试计划和测试用例

### 4.3.1 测试方法概述

本项目采用以下测试策略：

- **单元测试**：使用 JUnit 5 对后端 Service 层的核心业务方法进行单元测试；
- **接口测试**：使用 Postman 对 RESTful API 进行接口级别的功能测试和边界测试；
- **功能测试**：采用黑盒测试方法，在浏览器中模拟用户操作，验证前端页面交互逻辑；
- **兼容性测试**：在 Chrome、Edge、Safari 及移动端浏览器中验证页面显示与功能一致性。

### 4.3.2 核心模块测试用例

**模块一：用户认证模块**

| 用例编号 | 测试项 | 输入数据 | 预期输出 | 实际输出 | 结果 |
|----------|--------|----------|----------|----------|------|
| TC-AUTH-01 | 正确登录 | 合法用户名+密码 | 登录成功，返回 JWT Token | 符合预期 | 通过 |
| TC-AUTH-02 | 错误密码 | 合法用户名+错误密码 | 提示"用户名或密码错误" | 符合预期 | 通过 |
| TC-AUTH-03 | 队长注册 | 球队名称+用户名+密码 | 创建球队，生成邀请码 | 符合预期 | 通过 |
| TC-AUTH-04 | 队员注册 | 邀请码+用户名+密码 | 加入对应球队 | 符合预期 | 通过 |
| TC-AUTH-05 | Token过期 | 携带过期Token访问 | 返回401，跳转登录页 | 符合预期 | 通过 |

**模块二：训练中心模块**

| 用例编号 | 测试项 | 输入数据 | 预期输出 | 实际输出 | 结果 |
|----------|--------|----------|----------|----------|------|
| TC-TRN-01 | 队长发布训练 | 标题/地点/时间/内容 | 训练创建成功，列表展示 | 符合预期 | 通过 |
| TC-TRN-02 | 队员报名训练 | 选择"参加" | 报名状态更新为 ATTEND | 符合预期 | 通过 |
| TC-TRN-03 | 训练结束后报名 | 对已结束训练点击报名 | 按钮禁用，提示已结束 | 符合预期 | 通过 |
| TC-TRN-04 | 出勤率计算 | 参加3次/总训练5次 | 显示出勤率 60% | 符合预期 | 通过 |
| TC-TRN-05 | 普通队员发布训练 | MEMBER 角色调用创建接口 | 返回 403 无权操作 | 符合预期 | 通过 |

**模块三：比赛中心模块**

| 用例编号 | 测试项 | 输入数据 | 预期输出 | 实际输出 | 结果 |
|----------|--------|----------|----------|----------|------|
| TC-MCH-01 | 创建比赛 | 对手/时间/地点/赛事类型 | 比赛创建成功 | 符合预期 | 通过 |
| TC-MCH-02 | 录入多局比分 | 局1:25:20, 局2:23:25 | 比分正确展示 | 符合预期 | 通过 |
| TC-MCH-03 | 状态流转 | 未开始→进行中 | 状态更新，按钮变化 | 符合预期 | 通过 |
| TC-MCH-04 | 已结束比赛编辑 | 尝试修改已结束比赛比分 | 前端允许编辑（基于信任机制）| 符合预期 | 通过 |

**模块四：球员档案模块**

| 用例编号 | 测试项 | 输入数据 | 预期输出 | 实际输出 | 结果 |
|----------|--------|----------|----------|----------|------|
| TC-PLY-01 | 年级计算 | entryYear=2024, degree=本科 | 显示"大一" | 符合预期 | 通过 |
| TC-PLY-02 | 年级跨年计算 | entryYear=2023, 当前日期=2025年3月 | 显示"大二"（未过9月）| 符合预期 | 通过 |
| TC-PLY-03 | 毕业迁移 | 队长点击毕业迁移 | 状态变为 ALUMNI，数据保留 | 符合预期 | 通过 |
| TC-PLY-04 | 编辑他人资料 | MEMBER 编辑其他队员 | 仅允许编辑自己，他人信息只读 | 符合预期 | 通过 |

（配图建议：Postman 测试截图、前端页面操作截图、测试用例执行结果汇总表）

### 4.3.3 测试结果汇总

| 测试类型 | 用例总数 | 通过 | 失败 | 通过率 |
|----------|----------|------|------|--------|
| 单元测试 | XX | XX | XX | XX% |
| 接口测试 | XX | XX | XX | XX% |
| 功能测试 | XX | XX | XX | XX% |
| 合计 | XX | XX | XX | XX% |

## 4.4 结果分析

经测试，TeamHub 系统的核心功能模块均已正确实现，测试用例通过率 XX%。测试过程中发现的主要问题及修复情况如下：

（1）（填写测试中发现的问题，如"训练列表在数据量较大时加载缓慢""时光轴关联跳转未携带具体ID"等）；

（2）（填写修复措施）。

系统在桌面端 Chrome/Edge/Safari 及移动端浏览器中均能正常访问，响应式布局表现符合设计预期。

---

# 5 总结

## 5.1 用户反馈

TeamHub 开发完成后，在开发团队所在球队进行了为期 X 周的试用。试用期间共收集到 X 位队员的使用反馈，主要反馈内容如下：

（1）**训练报名功能**：队员表示训练列表清晰，报名操作简单，能够直观看到已报名人数，方便决定是否参加；

（2）**比赛战绩功能**：队长反馈比赛的多小局比分录入灵活，能够满足排球等多局制比赛的需求，历史比赛回顾方便；

（3）**时光轴功能**：已毕业队员认为时光轴记录了球队的重要时刻，具有很好的纪念意义和文化传承价值；

（4）**改进建议**：部分队员建议增加训练前自动提醒功能，避免忘记参加训练。

（配图建议：队员使用系统时的手机/电脑截图、球队群内的反馈聊天截图）

## 5.2 全文总结

本项目按照软件工程的方法论，完成了 TeamHub 院队数字档案系统的设计与开发。主要工作包括：

（1）**需求分析阶段**：通过调研学生体育团队的管理痛点，运用 NABCD 模型明确项目价值，绘制了 UML 用例图、时序图、数据流图和 E-R 图，使用 Figma 完成了系统原型设计；

（2）**设计阶段**：确定了前后端分离的 B/S 架构，设计了 13 张数据库表，完成了年级自动计算、JWT 认证、训练报名截止等关键算法的流程设计；

（3）**实现阶段**：基于 Spring Boot + Vue 3 技术栈完成了前后端编码，实现了训练中心、比赛中心、球员档案、发展中心、时光轴、球队设置六大核心模块；

（4）**测试阶段**：设计了 XX 个测试用例，覆盖用户认证、训练管理、比赛管理、球员档案等核心功能，测试通过率 XX%；

（5）**部署阶段**：将系统部署至云服务器，配置 Nginx 反向代理和 systemd 服务守护，实现了公网访问。

系统的不足之处包括：暂未实现分页加载（数据量大时影响性能）、缺少图床集成功能（图片上传门槛较高）、没有训练前自动提醒机制。未来将在这些方向继续优化，并考虑接入 ECharts 数据可视化、增加角色扩展（副队长/经理）等功能。

---

# 6 体会

> 以下模板供每位组员参考，请根据自己的真实经历撰写，避免套话。

**XXX 同学**，分工：后端架构设计与 API 开发

在本次项目中，我主要负责后端服务的搭建与核心 API 的开发。起初我认为 Spring Boot 项目搭建比较简单，但实际开发中遇到了跨域配置、JWT 过滤器链顺序、MyBatis Plus 复杂查询等多个问题。印象最深的是多球队隔离的实现——起初我们考虑通过中间表实现，后来发现直接在每张业务表增加 `team_id` 字段更加简洁高效。这次经历让我认识到，设计阶段多花时间在数据模型上，编码阶段会少走很多弯路。同时，我也深刻体会到团队协作中接口文档的重要性，前后端通过 Swagger/Knife4j 对接口进行约定，大幅减少了沟通成本。

**XXX 同学**，分工：前端页面开发与交互实现

我主要负责 Vue 3 前端项目的搭建和页面开发。项目中最大的挑战是响应式布局的适配——我们需要同时保证桌面端侧边栏的完整功能和移动端抽屉菜单的流畅体验。在实现发展中心的"全队自评卡片网格"时，我尝试了多种 CSS Grid 布局方案，最终确定了在桌面端三列、移动端一列的自适应方案。另一个困难是弹窗组件的状态管理，由于项目未深入使用 Pinia，多个弹窗的显隐状态分散在各个组件中，导致偶尔出现层级覆盖问题。这让我意识到，即使是中小型项目，统一的状态管理方案也是必要的。

**XXX 同学**，分工：需求分析、测试与文档

我在项目中主要负责需求分析、测试用例设计和文档撰写工作。在需求分析阶段，我们多次与球队队长和队员沟通，才真正理清了"训练报名截止后不可修改""比赛多小局比分"等细节需求，这让我认识到需求调研不能浮于表面。测试阶段我发现了很多边界情况，例如"队长传承后老队长是否需要重新登录""解散球队时级联删除的顺序"等，这些问题在编码阶段容易被忽略，但在测试阶段暴露出来。通过本次项目，我理解了软件工程中"测试驱动思维"的重要性——在编写测试用例时就要考虑各种异常边界，而不只是验证正常流程。

---

# 附录

## 附录 A：核心代码

（粘贴关键代码片段，如 JWT 过滤器、年级计算算法、训练报名接口等，注意代码格式与注释清晰）

## 附录 B：数据库建表 SQL

（粘贴 init.sql 中核心表的建表语句）

## 附录 C：Git 提交记录

（粘贴 Gitee/GitHub 的提交历史截图或 git log 输出）

## 附录 D：用户使用截图

（粘贴队员使用系统时的实际界面截图，建议包含桌面端和移动端）

---

# 报告格式速查表

| 项目 | 格式要求 |
|------|----------|
| 章标题 | 黑体小2号加粗居中 |
| 节标题（如 1.1） | 黑体4号加粗，字母/数字 Times New Roman 4号加粗 |
| 小节标题（如 1.1.1） | 黑体小4号加粗，字母/数字 Times New Roman 小4号加粗 |
| 正文 | 宋体小4号，行间距固定1.5倍，字符间距标准 |
| 表标题 | 位于表格上方，黑体小4号 |
| 表内容 | 宋体5号 |
| 图标题 | 位于图下方，黑体小4号 |
| 分页 | 章与章之间插入分页符 |
| 打印 | A4 双面打印 |
