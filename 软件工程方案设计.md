# TeamHub 院队数字档案系统 — 方案设计

> 本文档描述 TeamHub 的系统架构、功能设计与核心原则。
> 项目采用前后端分离架构，支持多球队隔离，面向学生体育团队提供数字化管理。

---

## 一、项目定位

**辅助 QQ 群存在的球队数字档案馆。**

- **QQ 群负责**：即时喊人、闲聊、发通知、传文件（网站不做这些）
- **网站负责**：训练考勤永久存档、比赛战绩结构化记录、球员成长追踪、球队历史编年、训练建议收集

**核心原则**：视频/图片全部走外部网盘/图床分享链接，网站本身不存储任何大文件，只存 URL 地址。

---

## 二、技术架构

### 2.1 整体架构

| 层级 | 技术栈 | 说明 |
|------|--------|------|
| 前端 | Vue 3 + Vite + TypeScript + Element Plus | Composition API + `<script setup>` 开发 |
| 后端 | Spring Boot 3.5.14 + Java 17 + MyBatis Plus 3.5.12 | RESTful API，Spring Security JWT 认证 |
| 数据库 | MySQL 8 | 13 张表，支持多球队隔离（`team_id`）|
| API 文档 | Knife4j 4.4.0 | 访问 `/doc.html` 查看接口文档 |

### 2.2 后端架构

- **认证方式**：JWT Token（Bearer Token），有效期 7 天
- **权限模型**：RBAC 双角色控制（`MEMBER` / `CAPTAIN`）
- **方法级权限**：`@PreAuthorize("hasRole('CAPTAIN')")` 限制队长专属操作
- **CORS 配置**：覆盖 localhost、127.0.0.1、局域网段（10.x、192.168.x）及云服务器地址

### 2.3 前端架构

- **状态管理**：登录状态通过 `localStorage` 存取 Token 和用户信息，各组件独立管理
- **请求封装**：Axios 统一封装，自动携带 `Authorization: Bearer` 头，401 自动跳转登录页
- **路由守卫**：未登录用户自动重定向到 `/login`，公开页面（登录/注册/找回密码）除外
- **响应式布局**：桌面端左侧可折叠侧边栏；移动端（< 768px）抽屉式汉堡菜单

---

## 三、网站整体架构（页面层级与导航）

### 一级导航栏（全局可见，登录后）

首页 | 训练中心 | 比赛中心 | 球员档案 | 发展中心 | 时光轴
（队长额外显示：球队设置、管理后台）

### 页面树状结构

**首页（`/`）**
└── 个人仪表盘：球员信息卡片、本周训练提醒、最近比赛、出勤率、最新自评、待处理建议、修改密码

**训练中心**
├── 训练列表页（`/training`）：所有历史训练，显示个人报名状态与已结束标记
├── 训练详情页（`/training/:id`）：单次训练信息、接龙报名区、已报名名单、出勤统计
└── 发布训练弹窗（仅队长）

**比赛中心**
├── 比赛列表页（`/match`）：所有比赛，按赛事类型筛选（全部/训练赛/新生杯/华工杯/其他）
├── 比赛详情页（`/match/:id`）：比赛信息、多小局比分、首发名单、比赛备注、视频回放链接
└── 创建比赛弹窗（仅队长）

**球员档案**
├── 球员列表页（`/player`）：标签切换「当打之年」/「功勋人物」
├── 球员详情页（`/player/:id`）：完整档案展示、毕业迁移按钮（队长）
└── 编辑资料页（`/player/:id/edit`）：本人可编辑自己；队长可编辑所有人

**发展中心（`/development`）**
├── 个人发展标签页：我的自评表单、历史自评时间线、队友给我的建议
├── 全队自评标签页：所有 ACTIVE 球员卡片网格，点击弹出队员详情（自评历史 + 建议 + 提交建议）
└── 球队建议标签页：建议墙、分类筛选、状态筛选、队长回应

**时光轴（`/timeline`）**
└── 时光轴首页：年份筛选、月份跳转、事件节点时间河流，点击展开详情/编辑

**球队设置（`/team-setting`，仅队长）**
└── 邀请码管理 + 修改队名 + 队长传承 + 成员管理 + 解散球队

**管理后台（`/admin`，仅队长）**
└── 被举报建议审核（忽略/删除）

**认证页面（公开访问）**
├── 登录页（`/login`）
├── 注册页（`/register`）：队长创建球队 / 队员邀请码加入
└── 找回密码页（`/reset-password`）：通过邀请码重置密码

---

## 四、功能模块详细设计

### 模块 1：训练中心

| 功能点 | 需求描述 |
|--------|----------|
| 发布训练 | 队长创建训练，填写标题、时间、地点、内容、视频网盘链接 |
| 接龙报名 | 队员在训练详情页选择状态：参加 / 请假 / 待定；请假可填理由 |
| 名单汇总 | 实时显示本次训练报名人数、请假人数、待定人数 |
| 出勤统计 | 自动计算个人本学期出勤率（参加次数 ÷ 总训练次数）|
| 动作视频 | 训练详情页展示网盘/B站/抖音等链接，队员训练前点击预习 |
| 报名截止 | 训练时间 3 小时后自动标记「已结束」，禁止修改报名状态 |

### 模块 2：比赛中心（核心模块）

| 功能点 | 需求描述 |
|--------|----------|
| 创建比赛 | 填写对手、时间、地点，选择赛事类型：训练赛 / 新生杯 / 华工杯 / 其他 |
| 多小局比分 | 支持动态增加局数。排球/羽毛球等可填多局比分；足球/篮球等只填一局总比分。一套系统兼容所有球类 |
| 首发名单 | 队长从队员列表选择上场人员与位置，以文字列表形式记录 |
| 比赛备注 | 战术总结、赛后反思文字记录 |
| 视频归档 | 比赛结束后粘贴网盘分享链接，网站只存链接 |
| 状态流转 | 未开始 → 进行中 → 已结束（仅队长可改状态）|
| 赛事分类筛选 | 比赛列表页支持按赛事类型横向滚动筛选 |
| 开放编辑 | 比分、首发、备注、视频：所有登录用户可编辑（无权限校验，基于球队信任机制）|

### 模块 3：球员档案

| 功能点 | 需求描述 |
|--------|----------|
| 双区展示 | 球员列表分为「当打之年」（ACTIVE）与「功勋人物」（ALUMNI）两个标签页 |
| 年级自动计算 | 数据库不直接存储"大二""大三"，只存入学年份与学位。系统根据当前日期自动推算年级（本科/硕士/博士）|
| 毕业迁移 | 队长一键将队员状态改为 ALUMNI。迁移后历史数据全部保留，仅状态变更 |
| 个人主页 | 展示号码、主打方向/位置、个人照片（外部图床链接）|
| 自助编辑 | 队员登录后可编辑自己的资料；队长可编辑所有人 |
| 队长标签 | 球员卡片和详情页上，队长显示红色「队长」标识 |
| 成员管理 | 队长可在球队设置中编辑任意成员信息（姓名/学历/入学年份/号码/位置）或直接删除成员 |

### 模块 4：发展中心（Development Hub）

**定位**：球员的"成长病历本" + 球队的"意见箱"。

#### 4A. 个人发展区

| 功能点 | 需求描述 |
|--------|----------|
| 球员自评 | 球员本人填写：分类（技术/体能/意识/态度/配合）、当前问题（必填）、改进目标（必填）、期望训练（选填）|
| 历史自评 | 以时光轴形式展示该球员历次自评内容，可见成长轨迹变化 |
| 队友建议墙 | 所有人可向某球员提建议。建议需选择分类，限 200 字，可选实名或匿名 |
| 建议互动 | 每条建议可被点赞（每人限一次，再点取消）；被建议球员可标记建议状态：已采纳 / 尝试中 / 暂不适用 |
| 防滥用机制 | 禁止空泛骂人（如"你真菜"无法提交，必须具体指出问题）；被多人举报的建议由队长审核删除 |

#### 4B. 全队自评区

| 功能点 | 需求描述 |
|--------|----------|
| 球员卡片网格 | 以卡片形式展示所有 ACTIVE 球员的头像、姓名、号码、位置、最新自评摘要 |
| 队员详情弹窗 | 点击卡片弹出详情：该球员完整自评历史时间线 + 收到的所有建议 + 给 TA 提建议表单（匿名可选）|

#### 4C. 球队训练建议区

| 功能点 | 需求描述 |
|--------|----------|
| 发起建议 | 所有人均可提交。需选择分类（训练内容/时间安排/战术方向/装备后勤/其他）与紧急程度（普通/紧急）|
| 建议展示墙 | 按时间倒序展示所有建议。支持按分类筛选、按状态筛选 |
| 队长回应 | 每条建议队长选择状态并说明理由：已阅待议 / 采纳 / 暂不采纳 / 已落实 |
| 队员附议 | 普通队员可对建议点赞，队长可查看该建议获得几人支持 |
| 独立存在 | 本区域不与训练中心联动，不自动生成训练计划 |

### 模块 5：时光轴（球队编年史）

| 功能点 | 需求描述 |
|--------|----------|
| 自动事件 | 系统根据其他模块操作自动生成节点：训练创建、比赛结束、新球员入队、球员毕业迁移等 |
| 人工事件 | 人人可发布，可关联到某次具体训练、某场具体比赛或某位具体球员 |
| 内容形式 | 标题 + 描述 + 最多 3 张图片链接 + 1 个网盘相册链接 |
| 时间河流 | 按时间倒序排列，自动事件以灰色细线简洁展示，人工事件以彩色标记突出展示 |
| 详情展开 | 点击事件标题查看详情，人工事件支持编辑，自动事件只读 |
| 年份筛选 | 支持按年份、月份快速定位历史事件 |
| 球队隔离 | 每个球队只能看到自己球队的事件 |

### 模块 6：球队设置（仅队长）

| 功能点 | 需求描述 |
|--------|----------|
| 邀请码管理 | 查看当前邀请码、有效期、剩余时间，支持重新生成（7 天有效）|
| 修改队名 | 输入新队名保存 |
| 队长传承 | 选择队员确认传承，老队长降级为队员，新队长升级，需重新登录 |
| 成员管理 | 表格展示所有成员，队长可编辑任意成员信息或直接删除 |
| 解散球队 | 仅当球队只剩队长一人时可解散，删除球队及所有关联数据 |

### 模块 7：管理后台（仅队长）

| 功能点 | 需求描述 |
|--------|----------|
| 被举报建议审核 | 表格展示被举报的个人建议，支持「忽略举报」和「删除」|

---

## 五、角色权限设计

| 功能 | 普通队员 | 队长 |
|------|----------|------|
| 查看首页、训练、比赛、球员、发展中心、时光轴 | ✅ | ✅ |
| 报名参加训练 | ✅ | ✅ |
| 提交个人建议（给某球员）| ✅ | ✅ |
| 提交球队训练建议 | ✅ | ✅ |
| 编辑自己的球员资料 | ✅ | ✅ |
| 编辑比赛比分/首发/备注/视频 | ✅ | ✅ |
| 发布训练、创建/编辑比赛 | ❌ | ✅ |
| 编辑他人球员资料 | ❌ | ✅ |
| 删除被举报的建议 | ❌ | ✅ |
| 队员毕业迁移 | ❌ | ✅ |
| 修改队名/传承队长/解散球队 | ❌ | ✅ |
| 进入管理后台 | ❌ | ✅ |
| 编辑比赛状态（未开始→进行中→已结束）| ❌ | ✅ |

---

## 六、核心设计原则

1. **弹窗优先，独立页面为辅**：发布训练、创建比赛、提交建议、编辑资料等操作以弹窗形式在当前页面完成，减少页面跳转，操作更连贯。

2. **发展中心三标签结构**：个人发展、全队自评、球队建议三标签并存。全队自评以卡片网格展示所有队员，点击卡片进入该队员的完整详情弹窗。

3. **时光轴全局视角**：作为球队历史主线独立存在，不依附于任何单一模块。

4. **零文件存储**：视频与图片全部使用外部网盘/图床分享链接，网站仅存储 URL 字符串，无存储与带宽压力。

5. **年级自动计算**：只存入学年份与学位，前端/后端根据当前日期自动推算年级，无需每年手动维护。

6. **比赛按赛事分类**：明确区分训练赛、新生杯、华工杯等不同性质的比赛，便于分类回顾与筛选。

7. **响应式布局**：桌面端左侧导航栏可折叠；移动端侧边栏隐藏，顶部汉堡按钮弹出抽屉菜单，内容区全部单列排列。

8. **多球队隔离**：通过 `team_id` 字段实现数据隔离，一套系统可支撑多支球队共用。

---

## 七、创新点

1. **年级自动增长机制**：基于入学年份 + 学位动态计算，告别每年手动修改"大二变大三"。

2. **毕业一键迁移**：球员离队不是删除，而是归档至「功勋人物」，实现球队历史不断层。

3. **发展中心三层结构**：个人自评与建议（个体成长）、全队自评卡片墙（团队视角）、球队训练建议区（民主反馈），覆盖「人-队-事」三个维度。

4. **时光轴强制关联机制**：人工事件可挂靠具体训赛或球员，区别于无意义的社交水贴，形成可追溯的球队编年史。

5. **多小局比分动态设计**：一套数据结构兼容单局制（足球篮球）与多局制（排球羽毛球乒乓球）。

6. **赛事分类管理**：训练赛、新生杯、华工杯分类归档，球队成长轨迹清晰可查。

7. **零存储成本架构**：大文件全走外部链接，网站只存文本与 URL，适合学生团队无运维投入场景。

8. **邀请码即找回密码凭证**：忘记密码时联系队长获取邀请码即可重置，无需额外短信/邮件系统。

9. **队长传承与解散机制**：支持队长职位和平交接，以及单人生效的球队解散，避免球队数据无人管理。
