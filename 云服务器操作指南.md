**备份文件在 `/opt/teamhub/backup/` 里。**

## TeamHub 服务器操作手册

### 一、文件位置速查

| 项目 | 路径 |
|------|------|
| **后端 jar** | `/opt/teamhub/teamhub-0.0.1-SNAPSHOT.jar` |
| **后端配置** | `/opt/teamhub/application.yml` |
| **后端日志** | `/opt/teamhub/app.log` |
| **数据库备份** | `/opt/teamhub/backup/teamhub_YYYYMMDD_HHMMSS.sql` |
| **备份日志** | `/opt/teamhub/backup.log` |
| **前端静态文件** | `/var/www/teamhub/dist/` |
| **Nginx 配置** | `/etc/nginx/sites-available/teamhub` |
| **systemd 服务** | `/etc/systemd/system/teamhub.service` |

---

### 二、后端服务管理

```bash
# 查看后端运行状态
systemctl status teamhub

# 重启后端
systemctl restart teamhub

# 停止后端
systemctl stop teamhub

# 查看后端实时日志
journalctl -u teamhub -f

# 或者看应用日志
tail -f /opt/teamhub/app.log
```

---

### 三、数据库备份管理

```bash
# 手动执行备份
/opt/teamhub/backup.sh

# 查看备份文件
ls -lh /opt/teamhub/backup/

# 查看备份日志
cat /opt/teamhub/backup.log

# 手动导入备份（恢复数据）
mysql -u teamhub -p teamhub < /opt/teamhub/backup/teamhub_20260504_175654.sql
```

**定时任务：** 每天凌晨 3 点自动备份，保留最近 7 天。

---

### 四、更新项目代码（以后改完代码要更新服务器）

**1. 后端更新**
```bash
# 本地打包后上传到服务器，然后执行：
systemctl restart teamhub
```

**2. 前端更新**
```bash
# 服务器上删除旧 dist，上传新的
rm -rf /var/www/teamhub/dist
# 然后本地上传新的 dist 文件夹
```

**3. Nginx 配置修改后重载**
```bash
nginx -t
nginx -s reload
```

---

### 五、MySQL 数据库操作

```bash
# 登录数据库
mysql -u teamhub -p
# 密码：

# 常用命令
SHOW TABLES;
DESCRIBE user;
SELECT * FROM user;
```

---

### 六、服务器连接

```bash
# SSH 连接
ssh root@124.222.51.177

# 如果本地 SSH 连不上，用腾讯云网页终端（OrcaTerm）
```
